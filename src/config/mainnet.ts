import { Config } from '.';

export const mainnetConfig: Config = {
    nearEnv: 'mainnet',
    nearRpcEndpoint: 'https://rpc.mainnet.near.org',
    refFinanceId: 'v2.ref-finance.near',
    contractRepo: 'https://github.com/Meteor-Wallet/ref-swap-proxy',
};
