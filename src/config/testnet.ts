import { Config } from '.';

export const testnetConfig: Config = {
    nearEnv: 'testnet',
    nearRpcEndpoint: 'https://rpc.testnet.near.org',
    refFinanceId: 'ref-finance-101.testnet',
    contractRepo: 'https://github.com/Meteor-Wallet/ref-swap-proxy',
};
